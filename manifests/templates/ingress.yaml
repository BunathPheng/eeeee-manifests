---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: eeeee                  # Ingress name
  labels:
    app: eeeee
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "eeeee.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: eeeee               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
